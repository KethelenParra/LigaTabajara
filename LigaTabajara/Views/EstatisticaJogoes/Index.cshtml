@model IEnumerable<LigaTabajara.Models.EstatisticaJogo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Jogador.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Partida.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gols)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Jogador.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Partida.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gols)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
